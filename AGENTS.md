# Tonos Repository Instructions

- Work from `E:\projects\env_test`.
- Use PowerShell commands and Windows paths.
- Dart and Flutter commands are allowed when relevant to the task. Prefer focused formatting, analysis, and test commands; avoid dependency fetching or device/release builds unless the task requires them.
- Use `apply_patch` for manual source-code edits.
- Preserve unrelated and user-authored working-tree changes.
- Do not use destructive Git or filesystem commands without explicit approval.
- Use `rg` and `rg --files` for repository searches.
