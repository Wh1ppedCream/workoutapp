// lib/screens/form_posing_page.dart
import 'package:flutter/material.dart';

class FormPosingPage extends StatelessWidget {
  const FormPosingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Form and Posing')),
      body: const Center(child: Text('Form and Posing content')),
    );
  }
}